export default function SafetyBanner() {
  return (
    <div className="safety-banner">
      Demonstration platform using synthetic patients and synthetic payer policies only — not a real coverage
      decision. Every determination requires sign-off by a qualified clinical reviewer.
    </div>
  );
}
